import CodeBlock from "@theme/CodeBlock";
import TimeoutExample from "@examples/models/llm/llm\_timeout.ts";

# 添加超时限制

默认情况下，LangChain 将无限期地等待模型提供商的响应。如果您想添加超时限制，可以在调用模型时传递一个 `timeout` 选项，单位为毫秒。例如，对于 OpenAI：

import IntegrationInstallTooltip from "@mdx\_components/integration\_install\_tooltip.mdx";

<IntegrationInstallTooltip></IntegrationInstallTooltip>

```bash npm2yarn
npm install @langchain/openai
```

<CodeBlock language="typescript">{TimeoutExample}</CodeBlock>
```

