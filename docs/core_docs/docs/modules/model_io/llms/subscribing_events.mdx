import CodeBlock from "@theme/CodeBlock";
import DebuggingExample from "@examples/models/llm/llm\_debugging.ts";

# 订阅事件


尤其是在使用代理时，LLM处理提示时可能会有很多来回交互。
对于代理，响应对象包含一个 intermediateSteps 对象，您可以打印它以查看它所采取的步骤概述。如果这还不够，并且您想要查看与LLM的每次交互，您可以在LLM中传递回调函数进行自定义日志记录（或任何其他您想要执行的操作）：

有关可用事件的更多信息，请参阅文档的 [回调](/docs/modules/callbacks/) 部分。

import IntegrationInstallTooltip from "@mdx\_components/integration\_install\_tooltip.mdx";

<IntegrationInstallTooltip></IntegrationInstallTooltip>

```bash npm2yarn
npm install @langchain/openai @langchain/community
```

<CodeBlock language="typescript">{DebuggingExample}</CodeBlock>


